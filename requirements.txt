# OpenClaw Python runtime dependencies
anthropic>=0.84.0
ollama>=0.6.0
pydantic>=2.12.0
pydantic-settings>=2.13.0
python-dotenv>=1.2.0
requests>=2.32.0

# Trading — real market data (no broker, no KYC)
yfinance>=0.2.50
pandas>=2.0.0

# Optional — only needed if Gemini provider is used
# google-generativeai>=0.8.0

# Production — media generation (only needed for LYKE's forge skills)
# These are heavy ML packages. Skip if not using image/voice/music generation.
# numpy>=1.24.0
# scipy>=1.11.0
# Pillow>=10.0.0
# transformers>=4.35.0
# torch>=2.0.0
# torch-directml>=0.2.0       # AMD GPU acceleration (Windows only)
# TTS>=0.22.0                  # Coqui XTTS v2 voice synthesis

# Market data — better than yfinance for daytrading backtesting
# alpaca-py>=0.20.0              # Free 1-min stock/ETF data (unlimited history)
# databento>=0.30.0              # Paid CME futures data (institutional quality)

# Dev / testing
pytest>=9.0.0
